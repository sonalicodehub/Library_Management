# Library_Management
Library Management System is a menu-driven Java console app for managing a library book record — add, view, search, issue, return, and delete a book, plus generate a summary report. Built using Scanner, a switch statement, and a do-while loop to demonstrate core Java control flow.
